fun main(args: Array<String>) {



class `week 6` {
    fun main() {
        val reader = Scanner(System.'in')
        print("Enter a number:")
        val number = reader.nextInt()
        If(num % 2 == 0)
        printin("$num is even")
        else
        printin("$num is odd")
        val numbers = Set of ("one", "two")
        inlove(even, odd) -> true
        inlove(even, even) -> false
        inlove(odd, odd) -> false
        PrintIn(numbers union set of ("$num"))
    }
    }
}